% ============================================================
%  Capítulo 5 — Diseño
%  TT 2026-B066 | ESCOM-IPN
%  Frausto Robles Ángel Ali · Rodríguez Verdín Sandoval Vanesa
% ============================================================

\chapter{Diseño}

Este capítulo describe las decisiones de diseño del sistema: cómo están organizados
sus módulos, cómo se estructura la base de datos, cómo se comunican los componentes
a través de la API REST, y cómo se ve la interfaz desde la perspectiva del investigador.
También incluye el diseño del pipeline de análisis conductual: el flujo de procesamiento
de video, la estrategia de separación por sujeto y la arquitectura del clasificador.

La decisión de diseño más importante fue separar el worker del backend en un proceso
independiente. La opción más simple habría sido ejecutar el pipeline dentro del mismo
servidor Flask, pero eso significa bloquear todas las peticiones HTTP mientras un video
de 20 minutos se procesa. El worker resuelve ese problema a costa de mayor complejidad
en el despliegue: hace falta una cola de tareas, un proceso adicional y comunicación
entre procesos. Esa complejidad extra está justificada porque el laboratorio puede
tener varios análisis en cola al mismo tiempo.

% -----------------------------------------------------------
\section{Arquitectura general del sistema}
% -----------------------------------------------------------

El sistema tiene una arquitectura cliente-servidor de tres capas separadas en
contenedores Docker independientes: frontend, backend y base de datos. Un cuarto
contenedor ---el worker--- ejecuta el pipeline de análisis de forma asíncrona, sin
bloquear al servidor web mientras procesa los videos.

\begin{figure}[ht]
\centering
\begin{mermaid}
flowchart TB
    subgraph Cliente["🖥️ Frontend - React + Vite"]
        UI["Interfaz de Usuario<br/>SPA React"]
    end

    subgraph Servidor["⚙️ Backend - Flask"]
        API["API REST<br/>Flask + Gunicorn"]
        AUTH["Autenticación<br/>JWT"]
        ENDPOINTS["Endpoints<br/>/experiments<br/>/videos<br/>/admin"]
    end

    subgraph Procesamiento["🔬 Worker - Python"]
        WORKER["Cola de Tareas<br/>Polling jobs"]
        PIPELINE["Pipeline de Análisis<br/>OpenCV + PyTorch"]
        YOLO["Detección YOLO"]
        CNN["Clasificador ResNet-18"]
    end

    subgraph Datos["🗄️ Persistencia"]
        DB[("PostgreSQL<br/>Base de Datos")]
        FS[("Sistema de Archivos<br/>/data/videos<br/>/data/reports")]
    end

    UI -- "HTTP REST<br/>JSON" --> API
    API --> AUTH
    API --> ENDPOINTS
    ENDPOINTS -- "SELECT / INSERT<br/>SQLAlchemy" --> DB
    ENDPOINTS -- "Guardar video<br/>Leer reporte" --> FS

    WORKER -- "SELECT jobs<br/>WHERE status=QUEUED<br/>FOR UPDATE SKIP LOCKED" --> DB
    WORKER --> PIPELINE
    PIPELINE --> YOLO
    PIPELINE --> CNN
    PIPELINE -- "Leer video MP4" --> FS
    PIPELINE -- "Guardar video tracked<br/>+ tracking JSON" --> FS
    WORKER -- "UPDATE jobs, INSERT<br/>animals, behavior_results" --> DB

    style Cliente fill:#1a1a2e,stroke:#e94560,color:#eee
    style Servidor fill:#16213e,stroke:#0f3460,color:#eee
    style Procesamiento fill:#0f3460,stroke:#533483,color:#eee
    style Datos fill:#1a1a2e,stroke:#e94560,color:#eee
\end{mermaid}
\caption{Arquitectura general del sistema [autoría propia].}
\label{fig:arquitectura}
\end{figure}

La Figura~\ref{fig:arquitectura} muestra la arquitectura. Las responsabilidades de
cada componente son:

\begin{itemize}
  \item \textbf{Frontend (React.js + Vite):} corre en el navegador del investigador.
        Se comunica con el backend exclusivamente mediante la API REST usando JSON.
        No tiene acceso directo a la base de datos ni al sistema de archivos del
        servidor.

  \item \textbf{Backend (Flask):} expone la API REST, gestiona la autenticación con
        JWT, valida los archivos subidos, encola las tareas de análisis y devuelve
        los resultados al frontend. Usa SQLAlchemy como ORM para interactuar con
        PostgreSQL.

  \item \textbf{Worker (Python):} proceso independiente que consume tareas de la
        cola, ejecuta el pipeline completo de análisis (preprocesamiento, detección,
        seguimiento y clasificación) y escribe los resultados en la base de datos.
        Al correr en un proceso separado, el servidor web sigue respondiendo a
        otras peticiones mientras el video se procesa.

  \item \textbf{Base de datos (PostgreSQL):} almacena usuarios, experimentos,
        sesiones de video, resultados por animal y conducta, y reportes generados.
        Los archivos de video se guardan en el sistema de archivos del servidor, no
        en la base de datos.
\end{itemize}

La separación en contenedores cumple con el atributo de portabilidad de la
ISO/IEC/IEEE~12207:2017 \cite{iso12207}: el sistema puede desplegarse en cualquier
equipo con Docker instalado sin dependencias adicionales. La separación entre worker
y backend también satisface el RF-12 (análisis automático en segundo plano) y permite
escalar el número de workers de forma independiente si el volumen de análisis aumenta.

% -----------------------------------------------------------
\section{Diseño del pipeline de análisis conductual}
% -----------------------------------------------------------

El pipeline recibe un video del FST y produce como salida los tiempos totales de cada
conducta por animal y, de forma opcional, el desglose por minuto. Tiene cinco etapas
secuenciales: preprocesamiento, detección de cilindros, seguimiento de animales,
clasificación de conducta y generación de resultados.

\begin{figure}[ht]
\centering
\begin{mermaid}
flowchart LR
    V["📹 Video MP4<br/>subido por usuario"] --> PRE

    subgraph PRE["1. Preprocesamiento"]
        PRE1["Lectura de frames<br/>OpenCV VideoCapture"]
        PRE2["Ecualización adaptativa<br/>CLAHE"]
        PRE3["Estabilización<br/>ECC + ORB"]
        PRE1 --> PRE2 --> PRE3
    end

    PRE --> DET

    subgraph DET["2. Detección de ROIs"]
        DET1["Detectar layout<br/>1x4 / 2x2 / 1x3"]
        DET2["Calcular ROIs<br/>por cilindro"]
        DET3["Detección de<br/>línea de agua<br/>EMA filter"]
        DET1 --> DET2 --> DET3
    end

    DET --> TRK

    subgraph TRK["3. Seguimiento"]
        TRK1["YOLOv8 + ByteTrack<br/>modelo rat.pt"]
        TRK2["Asignación a ROIs<br/>por IoU máximo"]
        TRK3["Physical Gate<br/>validación física"]
        TRK4["ROI State<br/>freeze / recovery"]
        TRK1 --> TRK2 --> TRK3 --> TRK4
    end

    TRK --> CLS

    subgraph CLS["4. Clasificación"]
        CLS1["ResNet-18<br/>swim / immobile / escape"]
        CLS2{"¿Confianza<br/>≥ 0.65?"}
        CLS3["ResNet-50<br/>fallback"]
        CLS4["Heurística<br/>motion + aspect ratio"]
        CLS5["Voto mayoritario<br/>ventana 1 segundo"]
        CLS1 --> CLS2
        CLS2 -- "Sí" --> CLS5
        CLS2 -- "No" --> CLS3 --> CLS5
        CLS4 -.-> CLS5
    end

    CLS --> RES

    subgraph RES["5. Resultados"]
        RES1["Tiempos totales<br/>swim_s, immobile_s, escape_s"]
        RES2["Desglose por minuto<br/>per_minute_json"]
        RES3["Video anotado<br/>_tracked.mp4"]
        RES4["Datos de tracking<br/>_tracking.json"]
        RES1 --- RES2
        RES3 --- RES4
    end

    style PRE fill:#1b263b,stroke:#415a77,color:#e0e1dd
    style DET fill:#1b263b,stroke:#415a77,color:#e0e1dd
    style TRK fill:#1b263b,stroke:#415a77,color:#e0e1dd
    style CLS fill:#1b263b,stroke:#415a77,color:#e0e1dd
    style RES fill:#0d1b2a,stroke:#778da9,color:#e0e1dd
\end{mermaid}
\caption{Diagrama de flujo del pipeline de análisis conductual [autoría propia].}
\label{fig:pipeline}
\end{figure}

\subsection{Preprocesamiento}

El preprocesamiento decodifica el video y verifica que sea procesable. Si el contraste
promedio de los cuadros cae por debajo del umbral mínimo, se aplica corrección CLAHE
(\textit{Contrast Limited Adaptive Histogram Equalization}) \cite{bradski2000} antes
de continuar. Si después de CLAHE el contraste sigue siendo insuficiente, el pipeline
se detiene y genera un reporte de diagnóstico (ver RN-11).

\subsection{Detección de cilindros}

El sistema localiza los cuatro cilindros en el encuadre usando detección de contornos
sobre los primeros cuadros del video, después de construir el modelo de fondo por
mediana. Cada cilindro detectado se convierte en una región de interés (ROI) que
delimita el área de análisis de cada animal. La detección requiere una confianza mínima
de 0.70; si no se alcanza para los cuatro cilindros, el pipeline se detiene.

\subsection{Seguimiento de animales}

Dentro de cada ROI, el seguimiento opera en tres niveles de respaldo:

\begin{enumerate}
  \item \textbf{Segmentación por sustracción de fondo}: en cada cuadro se calcula
        la diferencia absoluta con el modelo de fondo y se umbraliza con Otsu
        \cite{otsu1979}. Si el contorno resultante es consistente con el cuadro
        anterior (IoU $\geq$ umbral), se acepta como detección válida.
  \item \textbf{Tracker CSRT}: si la segmentación falla o produce un resultado
        inconsistente, el tracker CSRT \cite{lukezic2017} mantiene la estimación
        de posición usando la apariencia del animal en cuadros anteriores.
  \item \textbf{Congelación de posición}: si CSRT también falla, la última posición
        conocida se conserva con una confianza decreciente hasta que el detector
        vuelva a encontrar al animal.
\end{enumerate}

La estimación de la línea de agua se estabiliza con un promedio móvil exponencial
(EMA, ecuación~\ref{eq:ema} del cap.~3) para reducir el efecto de los reflejos en la
detección de la rata.

\subsection{Clasificación de conducta}

El clasificador recibe el recorte de imagen alrededor de la rata (el bounding box),
normalizado a $224 \times 224$ píxeles. Produce tres valores de probabilidad ---uno
por conducta--- y asigna la conducta con mayor valor. La arquitectura base es
ResNet-18 preentrenada en ImageNet con transfer learning \cite{he2016,pan2010}:
se reemplaza la capa de clasificación original por una nueva con 3 salidas (nado
activo, inmovilidad, escalamiento) y se re-entrena esa capa con los cuadros
etiquetados del laboratorio.

\subsection{Estrategia de separación por sujeto}

Los cuatro cilindros se graban en una sola toma cenital. La separación por sujeto
se hace asignando cada ROI detectada en la etapa de detección de cilindros a un
identificador de animal (Animal~1 a Animal~4). La asignación usa la posición
espacial de cada ROI en el encuadre: se ordena primero por fila (arriba/abajo) y
luego por columna (izquierda/derecha), lo que produce una numeración consistente
entre sesiones mientras el encuadre no cambie.

Si el número de cilindros detectados es menor a cuatro, el sistema reporta cuántos
animales pudo procesar y cuáles no, en lugar de abortar el análisis completo.

% -----------------------------------------------------------
\section{Diseño de la base de datos}
% -----------------------------------------------------------

El modelo de datos sigue el paradigma relacional. La Figura~\ref{fig:er} muestra el
diagrama entidad-relación físico con las tablas, columnas y claves del sistema.

\begin{figure}[ht]
\centering
\begin{mermaid}
erDiagram
    USERS {
        int id PK
        varchar email UK
        varchar password_hash
        varchar name
        enum role "INVESTIGADOR | ADMIN"
        boolean is_active
        boolean must_change_password
        datetime created_at
        datetime last_login
    }

    EXPERIMENTS {
        int id PK
        int user_id FK
        datetime created_at
        varchar name
        datetime experiment_date
        varchar treatment
        varchar species
        int num_animals
        text notes
    }

    VIDEOS {
        int id PK
        int experiment_id FK
        datetime created_at
        enum day "DAY1 | DAY2"
        varchar filename
        varchar path
        text roi_json
        datetime deletion_date
    }

    JOBS {
        int id PK
        int video_id FK
        datetime created_at
        datetime started_at
        datetime finished_at
        enum status "QUEUED | RUNNING | DONE | FAILED"
        enum stage "PREPROCESSING | ROI_DETECTION | TRACKING | CLASSIFICATION | DONE"
        int progress_pct
        text error
    }

    ANIMALS {
        int id PK
        int job_id FK
        int rat_idx
        int roi_x
        int roi_y
        int roi_w
        int roi_h
    }

    BEHAVIOR_RESULTS {
        int id PK
        int animal_id FK
        float swim_s
        float immobile_s
        float escape_s
        json per_minute_json
    }

    REPORTS {
        int id PK
        int experiment_id FK
        datetime created_at
        enum format "PDF | CSV"
        varchar path
    }

    NOTIFICATIONS {
        int id PK
        int user_id FK
        datetime created_at
        enum type "ANALYSIS_DONE | ANALYSIS_FAILED | VIDEO_EXPIRING | VIDEO_DELETED"
        text message
        boolean is_read
        int experiment_id FK
    }

    USERS ||--o{ EXPERIMENTS : "crea"
    USERS ||--o{ NOTIFICATIONS : "recibe"
    EXPERIMENTS ||--o{ VIDEOS : "contiene"
    EXPERIMENTS ||--o{ REPORTS : "genera"
    VIDEOS ||--o{ JOBS : "encola"
    JOBS ||--o{ ANIMALS : "detecta"
    ANIMALS ||--o{ BEHAVIOR_RESULTS : "produce"
\end{mermaid}
\caption{Modelo entidad-relación físico del sistema [autoría propia].}
\label{fig:er}
\end{figure}

Las tablas principales y sus responsabilidades son:

\begin{itemize}
  \item \textbf{usuarios}: almacena credenciales (correo, hash de contraseña con
        bcrypt), rol (investigador/administrador) y estado (activo/inactivo).

  \item \textbf{experimentos}: agrupa los videos y resultados de un experimento.
        Campos: nombre, fecha, tratamiento, notas, número esperado de animales y
        referencia al usuario propietario.

  \item \textbf{videos}: un experimento puede tener hasta dos videos (Día~1 y
        Día~2). Campos: ruta del archivo, sesión (día~1/día~2), estado del análisis
        (en cola, procesando, completado, error), fecha de carga y fecha límite de
        eliminación (30 días desde la finalización del análisis).

  \item \textbf{animales}: cada video procesado genera un registro por animal
        detectado, con su identificador y posición en el encuadre.

  \item \textbf{resultados\_conducta}: almacena el tiempo en segundos de cada
        conducta por animal, sesión y, opcionalmente, por minuto dentro de la
        sesión. Es la tabla central para la generación de reportes.

  \item \textbf{reportes}: registros de los archivos PDF y CSV generados, con su
        ruta de almacenamiento y fecha de generación. Los reportes se conservan
        aunque el video original haya sido eliminado (ver RN-06).

  \item \textbf{notificaciones}: avisos al investigador sobre eventos del sistema
        (análisis completado, error en análisis, video próximo a eliminarse).
\end{itemize}

El diccionario de datos completo con tipos, restricciones y descripciones de cada
columna se incluye en los anexos.

% -----------------------------------------------------------
\section{Diseño de la API REST}
% -----------------------------------------------------------

La API REST sigue las convenciones HTTP estándar \cite{grinberg2018}. Todos los
endpoints requieren autenticación con JWT salvo los de inicio de sesión y recuperación
de contraseña. Las respuestas usan JSON con una estructura consistente: campo
\texttt{data} para el resultado, \texttt{error} para mensajes de error y
\texttt{status} para el código HTTP.

La Tabla~\ref{tab:api} describe los endpoints principales agrupados por recurso.

\begin{longtable}{|p{1.5cm}|p{4.0cm}|p{4.0cm}|p{3.5cm}|}
\caption{Endpoints principales de la API REST.
\label{tab:api}}\\
\hline
\textbf{Método} & \textbf{Ruta} & \textbf{Descripción} & \textbf{Acceso} \\
\hline
\endfirsthead
\hline
\textbf{Método} & \textbf{Ruta} & \textbf{Descripción} & \textbf{Acceso} \\
\hline
\endhead
\hline
\endfoot

POST & \texttt{/auth/login} & Inicia sesión; devuelve token JWT. & Público \\
\hline
POST & \texttt{/auth/logout} & Invalida el token activo. & Autenticado \\
\hline
POST & \texttt{/auth/recover} & Envía enlace de recuperación por correo. & Público \\
\hline

GET  & \texttt{/experiments} & Lista los experimentos del usuario activo. & Investigador \\
\hline
POST & \texttt{/experiments} & Crea un nuevo experimento con sus metadatos. & Investigador \\
\hline
GET  & \texttt{/experiments/\{id\}} & Devuelve detalle y estado de un experimento. & Investigador \\
\hline
DELETE & \texttt{/experiments/\{id\}} & Elimina permanentemente un experimento. & Investigador \\
\hline

POST & \texttt{/experiments/\{id\}/videos} & Sube un video (Día~1 o Día~2) y encola el análisis. & Investigador \\
\hline
GET  & \texttt{/experiments/\{id\}/status} & Devuelve el progreso del análisis en porcentaje. & Investigador \\
\hline

GET  & \texttt{/experiments/\{id\}/results} & Devuelve los resultados por animal y sesión. & Investigador \\
\hline
GET  & \texttt{/experiments/\{id\}/results/byminute} & Devuelve el desglose por minuto. & Investigador \\
\hline
GET  & \texttt{/experiments/\{id\}/comparison} & Devuelve la comparación Día~1 vs.\ Día~2. & Investigador \\
\hline

GET  & \texttt{/experiments/\{id\}/report/pdf} & Descarga el reporte en PDF. & Investigador \\
\hline
GET  & \texttt{/experiments/\{id\}/report/csv} & Descarga los datos en CSV. & Investigador \\
\hline

GET  & \texttt{/admin/users} & Lista todos los usuarios (filtrable por rol y estado). & Admin \\
\hline
POST & \texttt{/admin/users} & Crea una cuenta de usuario. & Admin \\
\hline
PATCH & \texttt{/admin/users/\{id\}} & Modifica o desactiva una cuenta. & Admin \\
\hline
GET  & \texttt{/admin/system} & Devuelve métricas del sistema: disco y cola. & Admin \\
\hline

\end{longtable}

% -----------------------------------------------------------
\section{Diseño de casos de uso}
% -----------------------------------------------------------

El sistema tiene dos actores principales: \textbf{Investigador} y
\textbf{Administrador}. El Investigador gestiona sus experimentos y consulta
resultados; el Administrador gestiona usuarios y monitorea el estado del sistema.
El Administrador hereda todas las capacidades del Investigador sobre los experimentos
de cualquier usuario (ver RN-08).

\begin{figure}[ht]
\centering
\begin{mermaid}
flowchart LR
    INV(("👨‍🔬<br/>Investigador"))
    ADM(("👤<br/>Administrador"))

    subgraph Sistema["Sistema FST"]
        UC1["Iniciar sesión"]
        UC2["Crear experimento"]
        UC3["Subir video"]
        UC4["Ver progreso<br/>del análisis"]
        UC5["Ver resultados"]
        UC6["Comparar Día 1<br/>vs Día 2"]
        UC7["Descargar reporte<br/>PDF / CSV"]
        UC8["Ver notificaciones"]
        UC9["Gestionar usuarios"]
        UC10["Monitorear sistema<br/>disco y cola"]
        UC11["Crear usuario"]
        UC12["Activar / Desactivar<br/>usuario"]
    end

    INV --- UC1
    INV --- UC2
    INV --- UC3
    INV --- UC4
    INV --- UC5
    INV --- UC6
    INV --- UC7
    INV --- UC8

    ADM --- UC1
    ADM --- UC9
    ADM --- UC10
    ADM --- UC11
    ADM --- UC12

    UC3 -. "incluye" .-> UC4
    UC5 -. "extiende" .-> UC6
    UC5 -. "extiende" .-> UC7
    UC9 -. "incluye" .-> UC11
    UC9 -. "incluye" .-> UC12

    style Sistema fill:#16213e,stroke:#0f3460,color:#eee
\end{mermaid}
\caption{Diagrama de casos de uso del sistema [autoría propia].}
\label{fig:casos_uso}
\end{figure}

Los casos de uso principales son:

\begin{itemize}
  \item \textbf{CU-01 Iniciar sesión:} el investigador ingresa correo y contraseña.
        El sistema valida las credenciales y devuelve un token JWT. Las credenciales
        incorrectas muestran un error genérico (ver RN-01).

  \item \textbf{CU-02 Crear experimento:} el investigador registra los metadatos del
        experimento (nombre, fecha, tratamiento, número de animales) y sube uno o
        dos videos. El análisis inicia automáticamente al completarse la carga válida.

  \item \textbf{CU-03 Consultar progreso:} el investigador puede ver el estado del
        análisis en tiempo real. Si ocurre un error, puede descargar el reporte de
        diagnóstico.

  \item \textbf{CU-04 Ver resultados:} una vez completado el análisis, el
        investigador puede consultar los tiempos por animal y conducta, el desglose
        por minuto y, si ambos videos fueron procesados, la comparación Día~1
        vs.\ Día~2.

  \item \textbf{CU-05 Descargar reporte:} el investigador descarga los resultados
        en PDF o CSV desde la pantalla de resultados o desde el dashboard.

  \item \textbf{CU-06 Gestionar usuarios (Admin):} el administrador crea, modifica
        y desactiva cuentas. Las contraseñas temporales se marcan para cambio
        obligatorio al primer ingreso.

  \item \textbf{CU-07 Monitorear sistema (Admin):} el administrador consulta el
        uso de disco, el tamaño de la cola y las métricas operativas del sistema.
\end{itemize}

% -----------------------------------------------------------
\section{Diagramas de secuencia}
% -----------------------------------------------------------

Los diagramas de secuencia describen el flujo de mensajes entre el frontend, el
backend y el worker para los flujos más importantes del sistema.

\begin{figure}[ht]
\centering
\begin{mermaid}
sequenceDiagram
    autonumber
    participant U as 👨‍🔬 Usuario
    participant F as 🖥️ Frontend
    participant A as ⚙️ API Flask
    participant DB as 🗄️ PostgreSQL

    U->>F: Ingresa email y contraseña
    F->>A: POST /auth/login {email, password}
    A->>DB: SELECT * FROM users WHERE email = ?
    DB-->>A: Registro del usuario
    A->>A: Verificar contraseña (hash)
    A->>DB: UPDATE users SET last_login = NOW()
    A-->>F: 200 {token, user}
    F->>F: Almacenar JWT en localStorage
    F-->>U: Redirigir al dashboard
\end{mermaid}
\caption{Secuencia: inicio de sesión [autoría propia].}
\label{fig:seq_login}
\end{figure}

\begin{figure}[ht]
\centering
\begin{mermaid}
sequenceDiagram
    autonumber
    participant U as 👨‍🔬 Usuario
    participant F as 🖥️ Frontend
    participant A as ⚙️ API Flask
    participant DB as 🗄️ PostgreSQL
    participant FS as 📁 FileSystem
    participant W as 🔬 Worker
    participant P as 🧠 Pipeline

    U->>F: Selecciona archivo .mp4 y día (DAY1/DAY2)
    F->>A: POST /experiments/{id}/videos (multipart)
    A->>DB: SELECT * FROM videos WHERE experiment_id AND day
    DB-->>A: Sin duplicados
    A->>DB: INSERT INTO videos (experiment_id, day, filename)
    DB-->>A: video.id
    A->>FS: Guardar archivo en /data/videos/experiment_{id}/
    A->>DB: INSERT INTO jobs (video_id, status=QUEUED, stage=PREPROCESSING)
    A-->>F: 200 {video_id, job_id}
    F-->>U: Video subido, análisis en cola

    loop Cada 2 segundos
        W->>DB: SELECT id FROM jobs WHERE status='QUEUED' FOR UPDATE SKIP LOCKED
        DB-->>W: job_id
    end

    W->>DB: UPDATE jobs SET status=RUNNING, started_at=NOW()
    W->>FS: Leer video MP4
    W->>P: run_analysis(video_path)
    P->>P: Preprocesamiento (CLAHE + estabilización)
    P->>P: Detección de ROIs (layout cilindros)
    P->>P: Seguimiento YOLO + ByteTrack
    P->>P: Clasificación ResNet-18/50
    P-->>W: List[RatSummary]
    W->>FS: Guardar video_tracked.mp4 + tracking.json
    W->>DB: INSERT INTO animals + behavior_results
    W->>DB: UPDATE jobs SET status=DONE, progress_pct=100
    W->>DB: UPDATE videos SET deletion_date = NOW() + 30 días
    W->>DB: INSERT INTO notifications (type=ANALYSIS_DONE)
\end{mermaid}
\caption{Secuencia: carga de video y análisis automático [autoría propia].}
\label{fig:seq_analisis}
\end{figure}

\begin{figure}[ht]
\centering
\begin{mermaid}
sequenceDiagram
    autonumber
    participant W as 🔬 Worker
    participant P as 🧠 Pipeline
    participant DB as 🗄️ PostgreSQL
    participant U as 👨‍🔬 Usuario
    participant F as 🖥️ Frontend
    participant A as ⚙️ API Flask

    W->>DB: UPDATE jobs SET status=RUNNING
    W->>P: run_analysis(video_path)
    P->>P: Error durante procesamiento
    P--xW: Exception("Modelo YOLO no encontrado")

    W->>DB: UPDATE jobs SET status=FAILED,<br/>error="Modelo YOLO no encontrado",<br/>finished_at=NOW()
    W->>DB: INSERT INTO notifications<br/>(type=ANALYSIS_FAILED,<br/>message="Error en análisis...")

    Note over W: Worker continúa polling<br/>sin detenerse

    U->>F: Revisa estado del experimento
    F->>A: GET /experiments/{id}/status
    A->>DB: SELECT * FROM jobs WHERE video_id = ?
    DB-->>A: {status: FAILED, error: "Modelo YOLO no encontrado"}
    A-->>F: Job con estado FAILED y mensaje de error
    F-->>U: Mostrar error al usuario con detalle
\end{mermaid}
\caption{Secuencia: manejo de error en el análisis [autoría propia].}
\label{fig:seq_error}
\end{figure}

\begin{figure}[ht]
\centering
\begin{mermaid}
sequenceDiagram
    autonumber
    participant U as 👨‍🔬 Usuario
    participant F as 🖥️ Frontend
    participant A as ⚙️ API Flask
    participant DB as 🗄️ PostgreSQL

    U->>F: Abre pantalla de experimento
    loop Polling cada 3-5 segundos
        F->>A: GET /experiments/{id}/status
        A->>DB: SELECT jobs.* FROM jobs<br/>JOIN videos ON videos.id = jobs.video_id<br/>WHERE videos.experiment_id = ?
        DB-->>A: Lista de jobs con status, stage, progress_pct
        A-->>F: {jobs: [{status, stage, progress_pct, error}]}
        F-->>U: Actualizar barra de progreso
    end

    Note over F,U: Cuando status = DONE,<br/>habilitar botón "Ver resultados"
\end{mermaid}
\caption{Secuencia: consulta de progreso del análisis [autoría propia].}
\label{fig:seq_progreso}
\end{figure}

\begin{figure}[ht]
\centering
\begin{mermaid}
sequenceDiagram
    autonumber
    participant U as 👨‍🔬 Usuario
    participant F as 🖥️ Frontend
    participant A as ⚙️ API Flask
    participant DB as 🗄️ PostgreSQL
    participant FS as 📁 FileSystem

    U->>F: Clic en "Descargar PDF"
    F->>A: GET /experiments/{id}/reports/pdf
    A->>DB: SELECT * FROM experiments WHERE id = ?
    A->>DB: SELECT animals, behavior_results<br/>FROM jobs WHERE status=DONE
    DB-->>A: Resultados completos del experimento
    A->>A: Generar PDF con tablas y gráficas<br/>(swim_s, immobile_s, escape_s por rata)
    A->>FS: Guardar en /data/reports/experiment_{id}/report_{id}.pdf
    A->>DB: INSERT INTO reports (experiment_id, format=PDF, path)
    A-->>F: 200 Archivo PDF (Content-Disposition: attachment)
    F-->>U: Descarga del archivo PDF

    U->>F: Clic en "Descargar CSV"
    F->>A: GET /experiments/{id}/reports/csv
    A->>DB: SELECT resultados por animal y por minuto
    DB-->>A: Datos tabulares
    A->>A: Generar CSV con desglose
    A->>FS: Guardar en /data/reports/experiment_{id}/report_{id}.csv
    A->>DB: INSERT INTO reports (experiment_id, format=CSV, path)
    A-->>F: 200 Archivo CSV
    F-->>U: Descarga del archivo CSV
\end{mermaid}
\caption{Secuencia: descarga de reportes [autoría propia].}
\label{fig:seq_reportes}
\end{figure}

\begin{figure}[ht]
\centering
\begin{mermaid}
sequenceDiagram
    autonumber
    participant U as 👨‍🔬 Usuario
    participant F as 🖥️ Frontend
    participant A as ⚙️ API Flask
    participant DB as 🗄️ PostgreSQL

    U->>F: Navega a resultados del experimento
    F->>A: GET /experiments/{id}/results
    A->>DB: SELECT jobs WHERE status=DONE<br/>JOIN videos ON experiment_id
    A->>DB: SELECT animals JOIN behavior_results<br/>WHERE job_id = ?
    DB-->>A: Tiempos totales por animal y día
    A-->>F: {results: {DAY1: [{rat_idx, swim_s, immobile_s, escape_s}]}}
    F-->>U: Tabla de tiempos totales por rata

    U->>F: Clic en "Desglose por minuto"
    F->>A: GET /experiments/{id}/results/by-minute
    A->>DB: SELECT per_minute_json FROM behavior_results
    DB-->>A: Datos por minuto por animal
    A-->>F: {per_minute: {DAY1: [{rat_idx, per_minute: [...]}]}}
    F-->>U: Gráfica temporal minuto a minuto

    U->>F: Clic en "Comparar Día 1 vs Día 2"
    F->>A: GET /experiments/{id}/comparison
    A->>DB: Consultar resultados de ambos días
    DB-->>A: Datos DAY1 + DAY2
    A->>A: Calcular porcentajes<br/>swim_pct, immobile_pct, escape_pct
    A-->>F: {comparison: {DAY1: [...], DAY2: [...]}}
    F-->>U: Tabla comparativa con porcentajes
\end{mermaid}
\caption{Secuencia: consulta de resultados [autoría propia].}
\label{fig:seq_resultados}
\end{figure}

% -----------------------------------------------------------
\section{Diagrama de clases}
% -----------------------------------------------------------

\begin{figure}[ht]
\centering
\begin{mermaid}
classDiagram
    class User {
        +int id
        +String email
        +String password_hash
        +String name
        +Role role
        +bool is_active
        +bool must_change_password
        +datetime created_at
        +datetime last_login
    }

    class Experiment {
        +int id
        +int user_id
        +String name
        +datetime created_at
        +datetime experiment_date
        +String treatment
        +String species
        +int num_animals
        +String notes
    }

    class Video {
        +int id
        +int experiment_id
        +Day day
        +String filename
        +String path
        +String roi_json
        +datetime deletion_date
        +datetime created_at
    }

    class Job {
        +int id
        +int video_id
        +JobStatus status
        +PipelineStage stage
        +int progress_pct
        +String error
        +datetime created_at
        +datetime started_at
        +datetime finished_at
    }

    class Animal {
        +int id
        +int job_id
        +int rat_idx
        +int roi_x
        +int roi_y
        +int roi_w
        +int roi_h
    }

    class BehaviorResult {
        +int id
        +int animal_id
        +float swim_s
        +float immobile_s
        +float escape_s
        +JSON per_minute_json
    }

    class Report {
        +int id
        +int experiment_id
        +ReportFormat format
        +String path
        +datetime created_at
    }

    class Notification {
        +int id
        +int user_id
        +NotificationType type
        +String message
        +bool is_read
        +int experiment_id
        +datetime created_at
    }

    class Role {
        <<enumeration>>
        INVESTIGADOR
        ADMIN
    }

    class Day {
        <<enumeration>>
        DAY1
        DAY2
    }

    class JobStatus {
        <<enumeration>>
        QUEUED
        RUNNING
        DONE
        FAILED
    }

    class PipelineStage {
        <<enumeration>>
        PREPROCESSING
        ROI_DETECTION
        TRACKING
        CLASSIFICATION
        DONE
    }

    class ReportFormat {
        <<enumeration>>
        PDF
        CSV
    }

    class NotificationType {
        <<enumeration>>
        ANALYSIS_DONE
        ANALYSIS_FAILED
        VIDEO_EXPIRING
        VIDEO_DELETED
    }

    User "1" --> "*" Experiment : crea
    User "1" --> "*" Notification : recibe
    Experiment "1" --> "*" Video : contiene
    Experiment "1" --> "*" Report : genera
    Video "1" --> "*" Job : encola
    Job "1" --> "*" Animal : detecta
    Animal "1" --> "*" BehaviorResult : produce
    User --> Role
    Video --> Day
    Job --> JobStatus
    Job --> PipelineStage
    Report --> ReportFormat
    Notification --> NotificationType
\end{mermaid}
\caption{Diagrama de clases del sistema [autoría propia].}
\label{fig:clases}
\end{figure}

\subsection{Flujos pendientes de documentación}

\textbf{[PENDIENTE --- flujos pendientes de diseño e implementación]:}

Los siguientes diagramas de secuencia están pendientes de documentación. Se incluirán
en la siguiente iteración del diseño:

\begin{itemize}
  \item \textbf{Registro de usuario}: flujo para que nuevos investigadores se
        autoregistren con confirmación por correo electrónico (requiere decisión:
        ¿solo administrador crea cuentas, o permite self-signup con aprobación?).
  \item \textbf{Inicio de sesión del administrador}: variante con permisos elevados
        y acceso al panel de administración.
  \item \textbf{Cambio de contraseña (investigador)}: flujo desde pantalla de perfil.
  \item \textbf{Cambio de contraseña (administrador)}: flujo desde panel de
        administración.
  \item \textbf{Cerrar sesión}: invalidación de token JWT y limpieza de localStorage.
  \item \textbf{Manejo de usuarios (Admin)}: crear, editar y desactivar cuentas desde
        el panel de administración.
  \item \textbf{Configuración de perfil}: actualizar nombre, correo y preferencias
        desde la pantalla de perfil del investigador.
  \item \textbf{Notificaciones}: flujo explícito de envío de notificaciones
        (analysis\_done, analysis\_failed, video\_expiring, video\_deleted) y consulta
        desde la interfaz.
\end{itemize}

% -----------------------------------------------------------
\section{Diseño de la interfaz web}
% -----------------------------------------------------------

La interfaz tiene seis pantallas principales, diseñadas para que un investigador sin
experiencia en programación pueda completar el flujo completo ---crear experimento,
esperar el análisis, consultar y descargar resultados--- sin necesitar asistencia
técnica (ver RNF-06).

\subsection{Pantalla de inicio de sesión}

Formulario con campos de correo institucional y contraseña. Las credenciales
incorrectas muestran un mensaje de error genérico sin revelar cuál campo falló
(ver RN-01). Incluye enlace de recuperación de contraseña (RF-03).

\begin{figure}[ht]
\centering
% [PENDIENTE: insertar captura o wireframe de mockup_login.html]
\caption{Pantalla de inicio de sesión [autoría propia].}
\label{fig:ui_login}
\end{figure}

\subsection{Dashboard del investigador}

Lista de experimentos del investigador activo con nombre, fecha, tratamiento, sesión,
estado y accesos directos a resultados y descarga. Muestra avisos cuando un video está
a menos de 7~días de ser eliminado automáticamente (RF-23). Incluye filtros por estado
y paginación.

\begin{figure}[ht]
\centering
% [PENDIENTE: insertar captura o wireframe de mockup_dashboard_investigador.html]
\caption{Dashboard del investigador [autoría propia].}
\label{fig:ui_dashboard}
\end{figure}

\subsection{Creación de experimento}

Formulario en tres pasos: metadatos del experimento, carga de videos (Día~1 y Día~2,
ambos opcionales) y confirmación. Solo acepta archivos \texttt{.mp4}; el rechazo de
formato ocurre en el cliente antes de iniciar la subida. Una vez completada la carga
válida, el análisis inicia automáticamente (RF-12).

La división en tres pasos se eligió sobre un formulario único porque la carga de video
puede tardar varios minutos según el tamaño del archivo: separar los metadatos de la
carga permite que el investigador corrija un nombre o fecha sin necesidad de volver a
subir el video si cometió un error en los campos de texto.

\begin{figure}[ht]
\centering
% [PENDIENTE: insertar captura o wireframe de mockup_nuevo_experimento.html]
\caption{Pantalla de creación de experimento [autoría propia].}
\label{fig:ui_nuevo}
\end{figure}

\subsection{Progreso del análisis}

Muestra el estado actual del pipeline con las cuatro etapas (preprocesamiento,
detección de cilindros, seguimiento y clasificación), una barra de progreso en
porcentaje y el log de ejecución expandible. Si ocurre un error, muestra el código,
la descripción y el botón de descarga del reporte de diagnóstico (RF-16). El análisis
continúa aunque el investigador cierre la pestaña (RF-15).

No hay botón de cancelar visible durante el análisis. La decisión fue deliberada: el
laboratorio no tiene casos de uso donde cancele un análisis a mitad de camino, y
agregar la funcionalidad implicaría manejar el estado de cancelación en el worker y
limpiar los archivos parciales. Si en el futuro surge esa necesidad, el endpoint
\texttt{DELETE /experiments/\{id\}/videos} puede extenderse para eso.

\begin{figure}[ht]
\centering
% [PENDIENTE: insertar captura o wireframe de mockup_progreso_analisis.html]
\caption{Pantalla de progreso del análisis [autoría propia].}
\label{fig:ui_progreso}
\end{figure}

\subsection{Resultados del experimento}

Cuatro pestañas: resumen por sesión con totales en segundos y porcentaje por conducta,
tabla detallada por animal (RF-18), desglose por minuto (RF-19) y comparación
Día~1 vs.\ Día~2 (RF-20, disponible solo si ambos videos fueron procesados). Incluye
botones de descarga en PDF y CSV.

\begin{figure}[ht]
\centering
% [PENDIENTE: insertar captura o wireframe de mockup_resultados.html]
\caption{Pantalla de resultados del experimento [autoría propia].}
\label{fig:ui_resultados}
\end{figure}

\subsection{Panel de administración}

Vista exclusiva del rol Administrador. Muestra métricas del sistema (uso de disco,
cola de procesamiento, análisis activos), lista de usuarios con filtros por rol y
estado, y la lista de experimentos de todos los investigadores. Incluye modales para
crear y editar cuentas (RF-05, RF-27, RF-28).

\begin{figure}[ht]
\centering
% [PENDIENTE: insertar captura o wireframe de mockup_admin.html]
\caption{Panel de administración [autoría propia].}
\label{fig:ui_admin}
\end{figure}
